#!/system/bin/sh

# 代理核心选择：mihomo / sing-box
bin_name="mihomo"

# 是否启用 IPv6 支持：true / false
ipv6="true"

# 是否启用 QUIC 支持：true / false
quic="false"

# 热点客户端流量是否走代理核心（TUN）：true / false
# false 时热点流量将绕过 TUN，直接使用物理网络出口
hotspot="true"
